import { View } from 'react-native';
import RoutesMenu from './routes/Routes';

const App = () => {
  return (
    <View style={{flex: 1, marginTop: 40}}>
      <RoutesMenu />
    </View>  
  );
}

export default App;
